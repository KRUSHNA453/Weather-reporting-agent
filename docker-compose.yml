services:
  weather-agent:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        INSTALL_LLM: "true"
    image: weather-agent:latest
    container_name: weather-agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
